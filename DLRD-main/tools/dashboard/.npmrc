# Build (personal) machine only. Force the PUBLIC npm registry so this build
# never resolves packages from any internal/company registry, regardless of the
# machine's global npm config. The work machine never runs npm at all.
registry=https://registry.npmjs.org/
fund=false
audit=false
